class Events {
    constructor(data) {
      this.data = data
    }

}

module.exports = Events

function ourArray(array) {
    const arr = []
    
    array.forEach(aray => {
     arr.push(aray)
    })
    
    return arr;
  }